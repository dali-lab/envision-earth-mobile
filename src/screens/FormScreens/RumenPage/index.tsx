import React from 'react';
import { ScrollView, SafeAreaView } from 'react-native';
import { GlobalStyle } from '../../../styles';

const RumenPage = () => {
  return (
    <SafeAreaView style={GlobalStyle.container}>
      <ScrollView>

      </ScrollView>
    </SafeAreaView>
  );
};

export default RumenPage;
